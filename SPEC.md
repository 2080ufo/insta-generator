# Content Planner — Спецификация проекта

## Описание
Веб-приложение для планирования контента в социальных сетях. Позволяет создавать контент-план на несколько недель с генерацией тем и распределением по платформам.

---

## СТРУКТУРА ИНТЕРФЕЙСА

### 1. ШАПКА (фиксированная, сверху)

**Слева:**
- Кнопка с названием текущего проекта + иконка 📁
- При клике — выпадающий список проектов
- Кнопка "+" рядом — создать новый проект

**По центру/справа:**
- Кнопка с названием текущей недели (📅 "Первая неделя")
- При клике — выпадающий список недель:
  - Первая неделя
  - Вторая неделя
  - Третья неделя
  - Четвёртая неделя
  - "+ Добавить неделю"

**Справа:**
- Кнопка 📤 — экспорт данных в JSON
- Кнопка 📥 — импорт данных из JSON

---

### 2. ЛЕВАЯ ПАНЕЛЬ (фиксированная ширина ~380px)

**Заголовок:** "🎯 Темы для постов"

**Блок "Контекст для генерации":**
- Текстовое поле для описания бизнеса/направления
- Кнопки прикрепления:
  - 📎 Файл (PDF, DOC, TXT)
  - 🖼 Картинка (с превью)
  - 🎵 Аудио
  - 🔗 Ссылка
- Прикреплённые файлы отображаются под кнопками, можно удалять

**Блок "Основной язык":**
- Выпадающий список языков (🇷🇺 Русский, 🇺🇸 English, 🇩🇪 Deutsch, etc.)
- Определяет язык первой строки темы

**Блок "AI модель":**
- Выпадающий список AI моделей:
  - Claude Sonnet 4, Claude Opus 4
  - GPT-4o, GPT-4 Turbo, GPT-3.5
  - Gemini Pro, Gemini Ultra
  - Llama 3, Mistral Large

**Кнопка "✨ Сгенерировать темы":**
- Генерирует 5 новых тем
- Темы с галочкой/возможно остаются
- Темы без отметки удаляются

**Список тем (скроллируемый):**
Каждая тема содержит:
- Основной текст (на выбранном основном языке)
- Перевод (меньшим шрифтом, курсив)
- Селектор языка перевода справа (EN, DE, ES, FR, IT, PT, ZH, JA)
- Кнопки справа:
  - ✓ Принять (зелёная) — тема остаётся, переносится на следующие недели
  - ? Возможно (оранжевая) — тема остаётся
  - ✕ Удалить (красная) — тема исчезает с анимацией

**Логика тем:**
- При смене основного языка — первая и вторая строки меняются местами
- Темы с галочкой автоматически переносятся на все следующие недели
- Темы с "?" тоже остаются при генерации новых

---

### 3. ПРАВАЯ ПАНЕЛЬ (занимает оставшееся место)

**Карточки платформ:**
- Перетаскиваемые карточки
- Можно изменять размер (ручка в правом нижнем углу)
- Кнопка удаления при наведении

**Каждая карточка содержит:**
- Заголовок с иконкой и названием платформы
- Кнопки форматов (зависят от платформы):
  - Instagram: Пост, Stories, Reels, Карусель
  - Pinterest: Пин, Idea Pin, Доска
  - TikTok: Видео, Stories, LIVE
  - YouTube: Видео, Shorts, Community, Описание
  - Facebook: Пост, Stories, Reels, Группа
  - X/Twitter: Твит, Тред, Цитата
  - LinkedIn: Пост, Статья, Карусель
  - Telegram: Пост, Канал, Чат
  - И т.д.

**Кнопка "+" в правом нижнем углу:**
- Открывает панель выбора платформы
- 18 платформ с иконками:
  Instagram, Pinterest, TikTok, YouTube, Facebook, X/Twitter,
  LinkedIn, Telegram, WhatsApp, Snapchat, Threads, Reddit,
  ВКонтакте, OK.ru, Сайт, Блог, Email, Подкаст

---

### 4. КАРТОЧКИ ФОРМАТОВ (вытягиваемые)

**Вытягивание:**
- Клик на кнопку формата (Stories, Reels...) — создаётся отдельная карточка
- Карточка соединена с родительской "ниточкой" (кривая Безье)
- Кнопка в родительской карточке скрывается

**Карточка формата содержит:**
- Заголовок с названием формата (перетаскивание за него)
- Кнопка ✕ — вернуть обратно
- Поле "📋 Контекст" — идеи, заметки
- Поле "✨ Текст" — сгенерированный текст
- Оба поля сохраняются даже при сворачивании

**Сворачивание:**
- Перетащить карточку обратно на родительскую — она сворачивается
- Или нажать ✕
- Кнопка появляется снова, с галочкой если есть текст

**Линии-ниточки:**
- SVG, кривые Безье
- Обновляются при перетаскивании любой карточки

---

## ЛОГИКА ДАННЫХ

### Проекты
- Уникальный ID, название
- Количество недель (по умолчанию 4)
- Текущая неделя
- Данные по каждой неделе

### Недели
- Контекст, язык, AI модель
- Список тем (с флагами accepted/maybe)
- Вложения

### Карточки платформ
- ID, тип платформы
- Позиция (x, y)
- Размер (width, height)
- Извлечённые форматы

### Карточки форматов
- ID родительской карточки
- Название формата
- Позиция (x, y)
- Контекст (текст)
- Сгенерированный текст

---

## СОХРАНЕНИЕ

Все данные сохраняются в localStorage:
- `instaGeneratorProjects` — проекты и недели
- `instaGeneratorCurrentProject` — ID текущего проекта
- `instaGeneratorActiveCards` — карточки платформ
- `instaGeneratorFormatCards` — карточки форматов
- `instaGeneratorFormatTexts` — тексты форматов

**Экспорт/Импорт:**
- JSON файл со всеми данными
- Для синхронизации между устройствами

---

## СТИЛИСТИКА

- Тёмная тема (фон #0f0f1a, карточки #1a1a2e)
- Акцентный цвет: #667eea (фиолетовый)
- Зелёный для "принять": #2ecc71
- Оранжевый для "возможно": #f39c12
- Красный для "удалить": #e74c3c
- Шрифт: system-ui (Apple, Roboto)
- Скруглённые углы: 10-15px
- Плавные анимации: 0.2s

---

## ТЕХНОЛОГИИ

- Чистый HTML, CSS, JavaScript
- Без фреймворков и зависимостей
- localStorage для хранения
- SVG для линий-связей
- CSS Grid/Flexbox для лейаута

---

## ФАЙЛОВАЯ СТРУКТУРА

```
project/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
├── data/
│   └── (бэкапы JSON)
└── README.md
```
